Place your images here for the login screen.

Left panel background (first found is used):
  - ai-network.png, ai-network.jpg, ai-network.webp
If none exist, the left panel uses a CSS gradient.

Center column logo (icon only, no crop):
  - logo-icon.png  (preferred; Y-shaped symbol only, no text)
  - logo.png, logo.jpg, logo.webp, myway.png (fallbacks)
The login center uses logo-icon.png when present; run scripts/create_logo_icon.py to generate it from myway.png.
